# 序列化

*类型：practice ｜ 难度：进阶 ｜ 标签：yudao、Jackson、Serializable、JSON、Easy-Trans*

**yudao 的序列化实践围绕两条链路展开：JDK 原生序列化（`Serializable`，按需使用，能用 JSON 就不用）与 Jackson JSON 序列化（HTTP 接口的主力，通过全局 ObjectMapper 注册自定义序列化器 + 字段级注解 + yaml 配置三层叠加）。另集成 Easy-Trans 自动完成 ID 到名称的翻译。核心心智：字段注解优先级最高，全局模块次之，yaml 配置兜底。**

## 深入理解 Serializable 接口

### 核心概念

`Serializable` 是 Java 的标记接口，用于声明对象可参与 Java 原生序列化：

- 序列化：对象转字节流；反序列化：字节流还原对象。
- 未实现 `Serializable` 的对象在 `ObjectOutputStream.writeObject` 时会抛 `NotSerializableException`。

### 什么时候需要 Serializable

| 场景 | 是否需要 | 说明 |
| --- | --- | --- |
| 使用 `ObjectOutputStream` 持久化对象 | 需要 | 直接依赖 JDK 原生序列化 |
| Session/缓存底层使用 JDK 序列化器 | 需要 | 如 `JdkSerializationRedisSerializer` |
| REST 接口使用 Jackson | 不需要 | JSON 序列化与 `Serializable` 无关 |
| MQ 使用 JSON 转换器 | 不需要 | 由消息转换器处理 |

### 基本实现与读写流程

```java
import java.io.Serializable;

public class User implements Serializable {

    // 显式声明版本号，类结构变化时用于兼容性判断
    private static final long serialVersionUID = 1L;

    private Long id;
    private String name;

    // 不参与序列化
    private transient String token;
}
```

```java
import java.io.*;

public class SerializeFlowDemo {

    public static void main(String[] args) throws Exception {
        User user = new User();

        // 1) 写出对象
        try (ObjectOutputStream out = new ObjectOutputStream(new FileOutputStream("user.bin"))) {
            out.writeObject(user);
        }

        // 2) 读回对象
        try (ObjectInputStream in = new ObjectInputStream(new FileInputStream("user.bin"))) {
            User restored = (User) in.readObject();
            System.out.println(restored);
        }
    }

    static class User implements Serializable {
        private static final long serialVersionUID = 1L;
    }
}
```

### Spring 项目落地

- 检查 Redis/Session 是否使用 JDK 序列化器；若可调整，优先改为 JSON 序列化器。
- 仅保留确实走 JDK 序列化链路的类实现 `Serializable`。

```java
@Bean
public RedisTemplate<String, Object> redisTemplate(RedisConnectionFactory factory) {
    RedisTemplate<String, Object> template = new RedisTemplate<>();
    template.setConnectionFactory(factory);

    // 改为 JSON 序列化，降低对 Serializable 的依赖
    template.setDefaultSerializer(new GenericJackson2JsonRedisSerializer());
    return template;
}
```

常见问题速记：

| 问题 | 结论 |
| --- | --- |
| `Serializable` 是不是“对象可传输”通用标记 | 不是，只针对 Java 原生序列化 |
| `static` 字段会被序列化吗 | 不会 |
| `transient` 字段会被序列化吗 | 不会 |
| 为什么反序列化失败 | 常见原因是类版本不兼容或引用对象不可序列化 |

结论：`Serializable` 是“按需使用”的能力，不应默认加在所有类上；能用 JSON/Protobuf 的场景优先不用 JDK 原生序列化；决策标准是运行链路，而不是编码习惯。

## 深入理解 @JsonIgnore

`@JsonIgnore` 是 Jackson 注解，用于在 JSON 序列化和反序列化时忽略指定字段：序列化时该字段不出现在 JSON 输出中，反序列化时 JSON 中的该字段被忽略不赋值。

### 作用位置

| 位置 | 效果 |
| --- | --- |
| 字段上 | 序列化和反序列化都忽略 |
| Getter 方法上 | 仅序列化时忽略（不输出到 JSON） |
| Setter 方法上 | 仅反序列化时忽略（不从 JSON 读取） |

### 基本用法与单向忽略

```java
public class UserDTO {

    private Long id;
    private String username;

    @JsonIgnore  // 序列化和反序列化都忽略
    private String password;

    private String email;
}
```

序列化结果：

```json
{
  "id": 1,
  "username": "admin",
  "email": "admin@example.com"
}
```

仅序列化时忽略（允许从 JSON 读取，但不输出）更推荐用 `@JsonProperty(access = ...)` 精确控制：

```java
public class UserDTO {

    // WRITE_ONLY：只允许反序列化（从 JSON 读取），不序列化输出
    @JsonProperty(access = JsonProperty.Access.WRITE_ONLY)
    private String password;
}
```

### 常见使用场景

- 敏感信息：密码、token、密钥等。
- 内部字段：不需要暴露给前端的业务字段。
- 循环引用：避免双向关联导致的无限递归。

### 与 transient 的区别

| 特性 | @JsonIgnore | transient |
| --- | --- | --- |
| 作用范围 | 仅 Jackson JSON 序列化 | JDK 原生序列化 |
| 对 JSON 的影响 | 忽略 | 无影响（仍会序列化） |
| 对 JDK 序列化的影响 | 无影响（仍会序列化） | 忽略 |

## 深入理解 @JsonInclude

`@JsonInclude` 控制字段在序列化时的包含条件，常用于过滤 `null`、空值等不必要的字段，减少 JSON 体积。可标注在类或字段上，字段级别优先于类级别。

### Include 枚举值

| 枚举值 | 含义 |
| --- | --- |
| `ALWAYS` | 始终包含（默认行为） |
| `NON_NULL` | 非 null 时才包含 |
| `NON_EMPTY` | 非 null 且非空（空字符串、空集合等）时才包含 |
| `NON_DEFAULT` | 值不等于类型默认值时才包含（如 int 默认 0） |
| `CUSTOM` | 自定义过滤逻辑 |

### 基本用法

```java
@JsonInclude(JsonInclude.Include.NON_NULL)  // 类级别：所有 null 字段都不输出
public class UserDTO {

    private Long id;

    // 字段级别：为 null 时不输出
    @JsonInclude(JsonInclude.Include.NON_NULL)
    private String nickname;

    // 为 null 或空集合时不输出
    @JsonInclude(JsonInclude.Include.NON_EMPTY)
    private List<String> roles;
}
```

全局配置，避免每个类都加注解：

```java
@Configuration
public class JacksonConfig {

    @Bean
    public Jackson2ObjectMapperBuilderCustomizer customizer() {
        return builder -> builder.serializationInclusion(JsonInclude.Include.NON_NULL);
    }
}
```

```yaml
spring:
  jackson:
    default-property-inclusion: non_null
```

`NON_EMPTY` 比 `NON_NULL` 更严格，以下情况都会被过滤：`null`、空字符串 `""`、空集合 `[]`、空 Map `{}`、空数组、`Optional.empty()`。

最佳实践：全局配置 `NON_NULL` 是最常见的做法；需要区分 `null` 和空值语义时在字段上单独用 `NON_EMPTY`；避免混用全局配置和字段级注解造成行为不一致。

## 深入理解 @JsonIgnoreProperties

`@JsonIgnoreProperties` 是类级别注解，用于批量忽略属性，或在反序列化时忽略未知字段。

| 特性 | @JsonIgnore | @JsonIgnoreProperties |
| --- | --- | --- |
| 作用位置 | 字段/方法 | 类 |
| 忽略方式 | 单个字段 | 批量指定或忽略未知字段 |
| 忽略未知字段 | 不支持 | 支持（`ignoreUnknown = true`） |

### 批量忽略与忽略未知字段

```java
// 批量忽略指定字段
@JsonIgnoreProperties({"password", "token", "internalCode"})
public class UserDTO {

    private Long id;
    private String username;
    private String password;    // 序列化和反序列化都忽略
    private String token;
    private String internalCode;
}
```

```java
// 忽略未知字段：JSON 中包含对象没有的字段时不报错
// （默认会抛 UnrecognizedPropertyException）
@JsonIgnoreProperties(ignoreUnknown = true)
public class UserDTO {

    private Long id;
    private String username;
}
```

全局配置（推荐），等同于所有类 `ignoreUnknown = true`：

```java
@Bean
public Jackson2ObjectMapperBuilderCustomizer customizer() {
    return builder -> builder.featuresToDisable(
        DeserializationFeature.FAIL_ON_UNKNOWN_PROPERTIES
    );
}
```

```yaml
spring:
  jackson:
    deserialization:
      fail-on-unknown-properties: false
```

典型场景：对接第三方接口时对方返回的字段比本地 DTO 多，用 `ignoreUnknown = true` 避免报错，提高接口兼容性。

## 反序列化中为什么需要空构造方法

Jackson 反序列化时需要先创建对象实例再通过反射赋值，默认使用无参构造方法。缺少时会抛出：

```text
Cannot construct instance of `com.example.UserDTO` (no Creators, like default constructor, exist)
```

### 什么时候会缺少

显式定义了有参构造方法后，编译器不会自动生成无参构造方法：

```java
public class UserDTO {

    private Long id;
    private String username;

    // 显式定义了有参构造方法后，无参构造消失
    public UserDTO(Long id, String username) {
        this.id = id;
        this.username = username;
    }
}
```

### 解决方案

方案 1：手动添加无参构造方法。

方案 2：使用 Lombok 自动生成（最常用）：

```java
@Data
@NoArgsConstructor   // 生成无参构造方法
@AllArgsConstructor  // 生成全参构造方法
public class UserDTO {

    private Long id;
    private String username;
}
```

方案 3：`@JsonCreator` 指定构造方法，适用于不可变对象（`final` 字段）：

```java
public class UserDTO {

    private final Long id;
    private final String username;

    // 指定 Jackson 使用此构造方法反序列化
    @JsonCreator
    public UserDTO(@JsonProperty("id") Long id,
                   @JsonProperty("username") String username) {
        this.id = id;
        this.username = username;
    }
}
```

方案 4：`@JsonDeserialize` 自定义反序列化器，适用于复杂场景。

### JDK 原生序列化的区别

JDK 原生序列化（`ObjectInputStream`）不需要无参构造方法，它通过 `sun.reflect.ReflectionFactory` 绕过构造方法直接创建对象。

常见问题：

| 问题 | 原因 | 解决方案 |
| --- | --- | --- |
| 反序列化报 `no Creators` 错误 | 缺少无参构造方法 | 添加无参构造方法或使用 `@JsonCreator` |
| Lombok `@Data` 不生成无参构造 | 显式定义了其他构造方法 | 加上 `@NoArgsConstructor` |
| 不可变对象无法反序列化 | `final` 字段无法通过 setter 赋值 | 使用 `@JsonCreator` 指定构造方法 |

最佳实践：DTO/VO 类统一使用 `@NoArgsConstructor` + `@AllArgsConstructor`；不可变对象用 `@JsonCreator`；避免在构造方法中执行复杂逻辑。

## 项目中的 JSON 序列化

### 序列化过程

阶段一：应用启动时（配置阶段）：

```text
Spring Boot 启动
    ↓
读取 META-INF/spring/...AutoConfiguration.imports
    ↓
发现 HrJacksonAutoConfiguration
    ↓
调用 jsonUtils(List<ObjectMapper> objectMappers) Bean 方法
    ↓
创建 SimpleModule，注册所有自定义序列化器/反序列化器
    ↓
objectMappers.forEach(om -> om.registerModule(simpleModule))
    ↓
ObjectMapper 配置完成，等待使用
```

关键点：`List<ObjectMapper> objectMappers` 由 Spring 自动注入，其中最重要的是 Spring MVC 内部的 ObjectMapper，被 `MappingJackson2HttpMessageConverter` 持有。

阶段二：每次 HTTP 请求时（执行阶段）。序列化（Controller 返回数据 → JSON）：

```text
Controller 方法返回对象
    ↓
DispatcherServlet 找到 MappingJackson2HttpMessageConverter（支持 application/json）
    ↓
调用 ObjectMapper.writeValueAsString(returnValue)
    ↓
遇到 Long → NumberSerializer（防止 JS 精度丢失）
遇到 LocalDateTime → TimestampLocalDateTimeSerializer
    ↓
写入 HTTP 响应体
```

反序列化（JSON 请求体 → Java 对象）：

```text
HTTP 请求到达（@RequestBody 参数）
    ↓
MappingJackson2HttpMessageConverter 读取请求体
    ↓
调用 ObjectMapper.readValue(requestBody, TargetClass.class)
    ↓
遇到 LocalDateTime → TimestampLocalDateTimeDeserializer
    支持时间戳: 1710234600000 → LocalDateTime
    支持字符串: "2026-03-12 14:30:00" → LocalDateTime
    ↓
填充 Java 对象，传入 Controller 方法
```

触发点示例：

```java
@RestController
@Validated
public class AttendanceDailyController {

    // 反序列化触发点：请求进来时，JSON → 请求参数对象
    @GetMapping("/page")
    public CommonResult<PageResult<AttendanceDailyRespVO>> getPage(
            @Valid AttendanceDailyPageReqVO pageReqVO) {
        // ...
    }

    // 序列化触发点：返回对象时，响应 VO → JSON
    @GetMapping("/{id}")
    public CommonResult<AttendanceDailyRespVO> get(@PathVariable Long id) {
        return success(service.get(id));
    }
}
```

### 三种配置方式（按作用范围从大到小）

方式一：全局配置（修改 HrJacksonAutoConfiguration），适合所有接口都需要的统一规则：

```java
SimpleModule simpleModule = new SimpleModule();
simpleModule
    // 已有的全局配置
    .addSerializer(Long.class, NumberSerializer.INSTANCE)
    .addSerializer(LocalDateTime.class, TimestampLocalDateTimeSerializer.INSTANCE)
    .addDeserializer(LocalDateTime.class, TimestampLocalDateTimeDeserializer.INSTANCE)
    // 新增：例如 BigDecimal 保留两位小数
    .addSerializer(BigDecimal.class, new BigDecimalSerializer());
```

自定义序列化器模板：

```java
public class BigDecimalSerializer extends JsonSerializer<BigDecimal> {

    @Override
    public void serialize(BigDecimal value, JsonGenerator gen, SerializerProvider serializers)
            throws IOException {
        // 保留两位小数、四舍五入，以字符串输出避免精度问题
        gen.writeString(value.setScale(2, RoundingMode.HALF_UP).toPlainString());
    }
}
```

方式二：字段级配置（注解），适合某个字段需要特殊处理：

```java
public class EmployeeRespVO {

    // 使用项目内置序列化器：数据库存 "[\"1\",\"2\"]"，API 返回 ["1", "2"]
    @JsonSerialize(using = StringToArraySerializer.class)
    @JsonDeserialize(using = ArrayToStringDeserializer.class)
    private String skillIds;

    // 指定日期格式（覆盖全局配置）
    @JsonFormat(pattern = "yyyy-MM-dd")
    private LocalDateTime entryDate;

    // null 时不输出该字段
    @JsonInclude(JsonInclude.Include.NON_NULL)
    private String remark;

    // 重命名字段
    @JsonProperty("emp_name")
    private String empName;

    // 忽略该字段（不序列化、不反序列化）
    @JsonIgnore
    private String internalCode;
}
```

方式三：application.yaml 全局属性配置，调整 ObjectMapper 行为开关：

```yaml
spring:
  jackson:
    # 日期格式（对未自定义序列化器的 Date 类型生效）
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: Asia/Shanghai

    serialization:
      write-dates-as-timestamps: false  # 日期不输出时间戳
      fail-on-empty-beans: false        # 空对象不报错

    deserialization:
      fail-on-unknown-properties: false # 忽略 JSON 中多余的字段（推荐开启）

    # null 值处理：全局 null 字段不输出
    default-property-inclusion: non_null
```

注意：项目中 `LocalDateTime` 已被 `TimestampLocalDateTimeSerializer` 接管，`write-dates-as-timestamps` 对它不生效——自定义序列化器优先级更高。

### 优先级与场景速查

```text
字段上的 @JsonSerialize/@JsonFormat  （最高，覆盖一切）
    ↓
HrJacksonAutoConfiguration 注册的 SimpleModule
    ↓
application.yaml spring.jackson.*    （最低，兜底配置）
```

| 需求 | 配置方式 |
| --- | --- |
| 新增一个类型的全局转换规则 | `SimpleModule.addSerializer()` |
| 某个字段日期格式不同 | `@JsonFormat(pattern = "...")` |
| 某个字段不返回给前端 | `@JsonIgnore` |
| 某个字段为 null 时不输出 | `@JsonInclude(NON_NULL)` |
| 整个 VO 的 null 字段都不输出 | 类上加 `@JsonInclude(NON_NULL)` |
| 接收请求时忽略多余字段 | yaml: `fail-on-unknown-properties: false` |
| 数据库字符串 ↔ 前端数组 | `@JsonSerialize` + `@JsonDeserialize` |

## Easy-Trans 在项目中的使用

### 核心作用

自动将 ID 翻译为对应的名称，例如 `userId: 1` → 自动填充 `userName: "张三"`，无需手动写关联查询。

### 集成方式

依赖配置：

```xml
<dependency>
    <artifactId>easy-trans-spring-boot-starter</artifactId>  <!-- 核心 -->
</dependency>
<dependency>
    <artifactId>easy-trans-mybatis-plus-extend</artifactId>  <!-- MyBatis Plus 集成 -->
</dependency>
```

与 MyBatis Plus 集成的关键：`BaseDO` 实现 `TransPojo` 接口：

```java
// BaseDO.java
// Easy-Trans 会往对象里塞 transMap 字段，这里屏蔽掉避免 Jackson 报错
@JsonIgnoreProperties(value = "transMap")
public abstract class BaseDO implements Serializable, TransPojo {
    // ...
}
```

`TransPojo` 是 Easy-Trans 的标记接口，告诉框架该类可作为翻译的数据来源，Easy-Trans 通过 MyBatis Plus 的 Mapper 直接查这张表。

全局配置：

```yaml
easy-trans:
  # 关闭全局拦截（性能考虑），改为按需在方法上加注解
  is-enable-global: false
```

### 使用步骤

第一步：VO 类实现 VO 接口，字段上加 `@Trans`：

```java
public class OperateLogRespVO implements VO {  // 必须实现 VO 接口

    // 翻译配置：
    // type = SIMPLE：通过 MyBatis Plus 查 AdminUserDO 表
    // target：目标 DO 类（必须继承 BaseDO，即实现了 TransPojo）
    // fields：从目标 DO 取哪个字段的值
    // ref：翻译结果填入当前 VO 的哪个字段
    @Trans(type = TransType.SIMPLE, target = AdminUserDO.class, fields = "nickname", ref = "userName")
    private Long userId;

    private String userName;  // 翻译结果自动填入这里
}
```

第二步：触发翻译。方式一：`@TransMethodResult` 注解（推荐，AOP 自动触发）：

```java
@GetMapping("/page")
@TransMethodResult  // 方法返回后自动触发翻译
public CommonResult<PageResult<OperateLogRespVO>> pageOperateLog(...) {
    PageResult<OperateLogDO> pageResult = operateLogService.getOperateLogPage(pageReqVO);
    // 返回前，Easy-Trans 自动扫描结果中所有 @Trans 字段并填充
    return success(BeanUtils.toBean(pageResult, OperateLogRespVO.class));
}
```

方式二：`TranslateUtils.translate()` 手动触发，用于无法加注解的场景（如导出 Excel，返回值是 void）：

```java
List<OperateLogRespVO> voList = BeanUtils.toBean(list, OperateLogRespVO.class);
// 手动触发翻译
TranslateUtils.translate(voList);
ExcelUtils.write(response, "操作日志.xls", "数据列表", OperateLogRespVO.class, voList);
```

### 执行流程与原理

```text
Controller 方法返回
    ↓
@TransMethodResult AOP 拦截
    ↓
扫描返回值中所有实现 VO 接口的对象
    ↓
找到 @Trans 注解的字段（如 userId）
    ↓
TransType.SIMPLE：调用 AdminUserDO 对应的 MyBatis Plus Mapper
    执行：SELECT id, nickname FROM admin_user WHERE id IN (1, 2, 3)
    ↓
将 nickname 填入 userName 字段
    ↓
返回给前端
```

`TransType.SIMPLE` 能自动查数据库的原因在于 `easy-trans-mybatis-plus-extend`：

```text
target = AdminUserDO.class
    ↓
Easy-Trans 通过 Spring 容器找到 AdminUserDO 对应的 Mapper
    ↓
AdminUserDO 继承 BaseDO，BaseDO 实现 TransPojo
    ↓
框架知道这个 DO 有对应的 MyBatis Plus Mapper
    ↓
自动调用 selectByIds(ids) 批量查询，填充翻译结果
```

`BaseDO implements TransPojo` 是集成的核心，没有它 `TransType.SIMPLE` 就找不到数据来源。

常用 `@Trans` 参数：

```java
// 翻译单个字段
@Trans(type = TransType.SIMPLE, target = AdminUserDO.class, fields = "nickname", ref = "userName")
private Long userId;

// 翻译多个字段：同时取 nickname 和 avatar
@Trans(type = TransType.SIMPLE, target = AdminUserDO.class,
       fields = {"nickname", "avatar"}, ref = {"userName", "userAvatar"})
private Long userId;

// 跨模块时用 targetClassName（字符串指定，避免循环依赖）
@Trans(type = TransType.SIMPLE,
       targetClassName = "com.hsf.hr.module.system.dal.dataobject.user.AdminUserDO",
       fields = "nickname", ref = "userName")
private Long userId;
```
