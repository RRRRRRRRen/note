# 从输入 URL 到页面渲染，中间发生了什么？

*类型：knowledge ｜ 难度：进阶 ｜ 标签：浏览器、网络、缓存、渲染、面试 ｜ 更新：2026-09-10*

**「输入 URL 到渲染」是浏览器面试的总纲：缓存判定（Service Worker 拦截 → 强缓存）→ DNS 解析 → TCP 握手 → TLS → HTTP 请求（协商缓存随请求头走）→ 响应 → 渲染管线（解析 HTML 建 DOM/CSSOM → 渲染树 → 布局 → 绘制 → 合成）。性能优化的全部套路都是这两句话的展开：减少网络往返（强缓存 + 指纹文件名、CDN、HTTP/2）和不阻塞渲染管线（关键 CSS、JS 异步加载、动画只动合成属性）。缓存先强缓存后协商，强缓存命中时连 DNS 都不会发生；渲染成本三档位（回流＞重绘＞合成）的引擎机制见专题篇「回流与重绘」。**

## 全景流水线

这道题能答多深，取决于你能否把散落的知识点挂到一条主线上。从敲下回车开始，**缓存判定走在一切网络动作之前**：请求先被 **Service Worker** 拦截（注册了的话，它可以直接用本地资源应答）；随后是**强缓存判定**——`Cache-Control` 有效期内直接使用本地副本，**一个字节都不会发上网络，连 DNS 都不会发生**。判定未命中才走 DNS（浏览器缓存 → 系统 → 路由器 → 运营商 → 根/顶级/权威服务器）拿到 IP，TCP 三次握手建立连接（HTTPS 再加 TLS 握手协商密钥）；然后发送 HTTP 请求——**协商缓存**的校验头（If-None-Match / If-Modified-Since）就随这枚请求一起出发，由服务器裁定 304 复用还是 200 新内容。拿到响应后浏览器进入渲染阶段。

```text
全景流水线 / pipeline

输入 URL
  │
  ▼
Service Worker 拦截（可编程直接应答）
  │ 未拦截 / 放行
  ▼
强缓存判定：max-age 未过期？
  │── 命中：不发任何网络请求 ──────────────┐
  │                                        │ 本地副本同样进入解析
  ▼ 未命中                                 │
DNS 解析（多级缓存 → 权威服务器）           │
  │                                        │
  ▼                                        │
TCP 三次握手 + TLS 握手                     │
  │                                        │
  ▼                                        │
发送 HTTP 请求（协商缓存头随请求走）        │
  │ 带 If-None-Match / If-Modified-Since   │
  ▼                                        │
服务器比对 ETag / Last-Modified ── 未变更 → 304 ──┐
  │                                              │
  │ 无校验头 → 直接 200                           │
  ▼                                              ▼
HTTP 响应（200 新内容 / 304 复用）
  │
  ▼
解析 HTML → DOM 树 + CSSOM 树
  │
  ▼
合成渲染树（排除 display:none）
  │
  ▼
布局 Layout（计算几何 → 回流）
  │
  ▼
绘制 Paint（像素填充 → 重绘）
  │ 分层上传 GPU
  ▼
合成 Composite（GPU 层合成）
```

渲染管线的五个阶段是所有「为什么页面卡」问题的根：解析（HTML/CSS 变成 DOM 与 CSSOM）、渲染树合成、布局（算几何位置）、绘制（填充像素）、合成（GPU 把各层拼成最终画面）。改几何属性触发全链路，改颜色跳过布局，改 transform/opacity 连绘制都跳过——只走合成。这个「管线跳级」就是动画优化的底层逻辑。

典型网络下的阶段耗时量级直觉（实际取决于 RTT、缓存与资源形态）：

```text
DNS 解析    ████████████████████████████████████ 87ms
TCP 握手    ████████ 28ms
TLS 握手    ██████████████████ 56ms
请求 + 响应 █████████████ 42ms
```

## 缓存体系：面试必考的主战场

缓存是这场考试里区分度最高的部分。判定顺序固定，且先于任何网络请求：**先查强缓存**——`Cache-Control: max-age` 在有效期内直接用本地副本，一个字节都不问服务器（`no-cache` 不是不缓存，而是「跳过强缓存、每次都协商」；`no-store` 才是真不存）；**强缓存过期后走协商缓存**——带上 `If-None-Match`（对应响应里的 ETag）或 `If-Modified-Since`（对应 Last-Modified）问服务器「资源变了吗」，没变返回 304 复用本地文件，变了返回 200 加新内容。

为什么有了 Last-Modified 还要 ETag？因为 mtime 只有秒级精度（一秒内多次修改检测不到）、内容不变仅 mtime 变会误判、分布式集群各机器 mtime 可能不一致。ETag 基于内容指纹（hash 或 inode+size+mtime 组合）解决这三个问题，两者同时存在时 **ETag 优先**。工程实践的最佳组合是：**带内容指纹的文件名 +**`Cache-Control: max-age=31536000, immutable`——文件变了名字就变，等于永远命中强缓存，协商缓存只在 HTML 入口上使用。

缓存判定链：

```text
Service Worker → 强缓存 → DNS + 建连 → 协商缓存
（可编程拦截，  （max-age 内   （强缓存未命中  （校验头随请求走，
 可短路应答）    直接用，不发    才发生）        304/200）
                请求）
```

| 维度 | 强缓存 | 协商缓存 |
| --- | --- | --- |
| 决策方 | 浏览器本地，服务器不参与 | 服务器裁定，需要一个 RTT |
| 命中表现 | from memory/disk cache，0 RTT | 304 Not Modified（空体） |
| 控制 Header | Cache-Control / Expires | ETag/If-None-Match、Last-Modified/If-Modified-Since |
| 过期后 / 适用对象 | 转入协商缓存继续判断 | HTML 入口（保证新版本可被发现） |

反例与正解对照：

```text
【反例】
Cache-Control: no-cache
# 用在所有资源上 → 每次都协商
# 高频访问的 JS/CSS 全在浪费往返
问题：全部资源都协商缓存，强缓存形同虚设，304 满天飞，延迟至少一个 RTT

【正解】
# JS/CSS：文件名带指纹，一年强缓存
app.a3f9c2.js → max-age=31536000, immutable
# HTML 入口：永远协商，保证新版本能被发现
index.html  → no-cache
要点：指纹文件名让「永久缓存」安全——内容变即名字变，新版本经 HTML 入口发现
```

> **记忆卡：缓存口诀**
> 先强后协商；`no-cache` 是「每次协商」、`no-store` 才是不缓存；ETag 优先于 Last-Modified；**指纹文件 + 长强缓存 + HTML 入口协商**是现代前端的标配组合。

## 渲染成本的三档位

渲染管线的成本递减：**回流**（几何变化，重新布局，最贵）→ **重绘**（外观变化，重新填充像素）→ **合成**（GPU 拼层，几乎免费）。改 width/top 走满整条管线；改 color/background 跳过布局；transform/opacity 由合成器线程直改，主线程卡死照样播放。这条成本线背后有一套完整的引擎机制——脏位标记、失效传播边界、读写交错的强制同步布局——全部展开在独立专题「**为什么改一个样式会引发重排**」一篇里，含布局抖动模拟器与度量手段。

同规模改动的相对成本直觉（触发清单与引擎机制见专题篇）：

```text
回流  ████████████████████████████████████████ 100%
重绘  ██████████████ 35%
合成  ██ 5%
```

## HTTP 演进与安全轮廓

```text
HTTP/1.0 ──▶ HTTP/1.1 ──▶ HTTP/2 ──▶ HTTP/3
短连接，每   长连接 ·      二进制分帧 ·  QUIC over UDP ·
请求一次握手 管道未启用 ·  多路复用 ·    0-RTT · 流独立重传
             队头阻塞      HPACK
```

HTTP/1.1 的核心痛点是**队头阻塞**：一个 TCP 连接上响应必须按序返回，前一个没完后面的全等着（浏览器只能靠开 6 个连接缓解）。HTTP/2 用**二进制分帧**把报文拆成帧、在一条连接上多路复用，帧属于哪个响应由流 ID 标注，互不排队；头部压缩（HPACK）顺带解决了 1.1 时代重复 header 的浪费。HTTP/2 的残余问题在 TCP 层——丢包时整条连接的所有流都要等重传，于是 HTTP/3 换用基于 UDP 的 QUIC，在传输层彻底解决。两条泳道直观感受「串行」与「帧交错」的差别：

```text
【HTTP/1.1 · 一条连接 · 响应必须按序（A 慢，B/C 全等）】 总耗时 ≈ 300ms
┌──────────────────┬──────────────┬──────────────┐
│ 请求 A · 100ms    │ B 等待 A      │ C 等待 B      │
└──────────────────┴──────────────┴──────────────┘

【HTTP/2 · 同一条连接 · 帧交错，按流 ID 重组】 总耗时 ≈ 100ms
┌──────┬──────┬──────┬──────┬──────┬──────┐
│  A1  │  B1  │  C1  │  A2  │  B2  │  C2  │
└──────┴──────┴──────┴──────┴──────┴──────┘
A1/B1/C1 是三个响应交错的帧，流 ID 标注归属——慢响应不再堵住别人；
但 TCP 层丢包仍会让所有流一起等重传，这就是 HTTP/3 换 QUIC 的原因。
```

HTTPS = HTTP + TLS：用**非对称加密**（证书公钥）交换**对称加密**的会话密钥，之后全部流量走对称加密（性能好）；证书由 CA 签名防中间人。

与「响应拿到手之后」这条主线交汇的还有一类话题——**安全**，本篇只勾轮廓。两大高频攻击的信任模型完全相反：**XSS** 把恶意脚本当成本站代码执行，利用的是「站点对输出的信任」，防御走输出转义、CSP、HttpOnly cookie；**CSRF** 让第三方页面借用户 cookie 发伪造请求，利用的是「服务器对请求来源的信任」，防御走 CSRF token、SameSite cookie、校验 Origin。一句话分野：一个防「注入执行」、一个防「伪造请求」——展开属于独立的 Web 安全专题，这里记住信任模型的分野即可。

## 经典追问链

脚本加载时序的正误对照：

```html
<!-- 【反例】不感知解析流水线：脚本在 DOM 未就绪时执行 -->
<head>
  <script>useFooter();</script> <!-- body 还没解析，undefined -->
</head>

<!-- 【正解】懂了「解析→执行」的时序，加载策略才有依据 -->
<script defer src="app.js"></script>
<!-- defer：HTML 解析完才执行，且保持声明顺序 -->
```

**Q1：强缓存和协商缓存的判断顺序？no-cache 和 no-store 区别？**

热身题，缓存体系的准入门槛——no-cache 的语义错误是每年都能刷掉一批人的经典坑。

先强缓存后协商：max-age 有效期内直接用本地副本（不发请求）；过期后带 If-None-Match/If-Modified-Since 发协商请求，未变返回 304。no-cache 是「可以缓存但每次使用前必须协商」；no-store 才是彻底不缓存——把 no-cache 理解成「不缓存」是最常见的面试事故。

加分项：强缓存命中在 DevTools 里显示为 from memory cache（短周期，渲染进程内存）或 from disk cache（长周期，磁盘），两者的分配策略浏览器自定，不是规范强制的。

**Q2：为什么有了 Last-Modified 还需要 ETag？两者冲突时听谁的？**

考协商缓存设计动机——答出「秒级精度」算合格，能把分布式一致性问题带出来的是加分层。

mtime 方案有三个盲区：精度只有秒级（一秒内多次修改检测不到）；内容没变但 mtime 变了会误判；分布式集群各节点 mtime 不一致导致协商结果摇摆。ETag 用内容指纹解决这三个问题，两者同时下发时按规范 ETag 优先（If-None-Match 优先于 If-Modified-Since）。

加分项：ETag 的代价是服务器要做内容 hash（CPU 开销），大文件场景 nginx 默认用 size+mtime 组合而非全量 hash——这也是「ETag 是怎么生成的」的真实答案。

**Q3：HTTP/2 的多路复用为什么能解决队头阻塞？彻底解决了吗？**

考协议演进的理解层次——多数人停在「2.0 有多路复用」，能指出 TCP 层残余问题的才到进阶线。

HTTP/1.1 的响应在一个连接上必须按序完整返回，前面的响应慢，后面全部排队——这是应用层的队头阻塞。HTTP/2 把报文拆成二进制帧，一条 TCP 连接上多个流交错传输、按流 ID 重组，单个慢响应不再堵住别人。但没彻底解决：TCP 只认识字节流，一旦丢包，内核要等重传补齐才能交付后续字节——所有流一起卡住，这是传输层的队头阻塞，最终由 HTTP/3 用 UDP 基础上的 QUIC（流间独立重传）解决。

加分项：HTTP/1.1 其实有过 pipeline 尝试，但因「响应必须按序」且中间代理兼容性差，浏览器默认从未开启——「1.1 支持管道化」而无「启用」是另一个考点陷阱。

**Q4：接手一个首屏很慢的项目，你的排查和优化路径是什么？**

体系化压轴题，把缓存、渲染、网络全部串起来——考察的是方法论而不是背优化清单。

先测量再动手：Network 面板看瀑布图（DNS/TLS/首字节/下载各占多少，谁在阻塞）、Performance 面板看主线程（长任务在哪）、Lighthouse 看 FCP/LCP 指标。然后按瓶颈对症下药：网络慢→CDN、HTTP/2、关键资源 preload；体积大→代码分割、按需加载、压缩与图片格式优化；渲染慢→关键 CSS 内联、JS 加 defer/async、长列表虚拟化、动画走 transform；缓存差→指纹文件名+强缓存+HTML 协商。每改一项都要回测量验证，优化不做无功之劳。

加分项：进阶认知，LCP 大头通常是「最大内容元素」的图片与字体，font-display: swap、图片 preload + 现代格式（WebP/AVIF）往往比压缩 JS 更立竿见影。

## 延伸阅读

- 为什么改一个样式会引发重排：回流与重绘——渲染成本怎么一档档算出来：脏位、失效传播、布局抖动与度量手段。
- 为什么有了索引还要回表？——「缓存与检索成本」思想在数据库侧的形态：B+ 树、回表与覆盖索引。
- setState 之后 React 做了什么？——渲染之上的调度：框架如何入队、合并并驱动这些 DOM 更新。
